export const SYNC_ERRORS = {
  NOT_FOUND: "Sync record not found",
  ALREADY_RUNNING: "Sync already in progress",
  TRIGGER_FAILED: "Failed to trigger sync",
} as const;

export const SYNC_DEFAULT_TOTAL = 100;
